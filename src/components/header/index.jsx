import {
    HeaderSection,
    HeaderTitle,

} from './styles';

const Header = () => (
    <HeaderSection>
        <HeaderTitle>Seu Perfil</HeaderTitle>
    </HeaderSection>
);

export default Header;